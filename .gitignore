# This repo holds recipes, not artefacts -- images live on GHCR, so anything
# pulled down locally does not belong in a commit.
*.sif
*.tar
*.tar.gz

.DS_Store
*.swp

# A stray `python smoke.py` outside the container leaves one of these next to
# the recipe; CI runs it with -B and never writes one.
__pycache__/
